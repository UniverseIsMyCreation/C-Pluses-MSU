Класс Rect

Реализовать класс Rect, описывающий прямоугольник на плоскости.

Класс имеет четыре закрытых поля: x, y типа int, wd, hd типа unsigned int. Это соответственно координаты левого верхнего угла, ширина и высота прямоугольника.

В классе реализовать следующие конструкторы:

конструктор с параметрами (int, int, unsigned int, unsigned int);
конструктор по умолчанию, в этом случае координаты левого верхнего угла должны равняться нулю, ширина и высота - единице;
конструктор преобразования неотрицательного целого числа в прямоугольник, в этом случае координаты левого верхнего угла должны равняться нулю, а ширина и высота - заданному неотрицательному целому числу.
Класс должен обладать следующими методами:

void print_corner() - печать двух координат левого верхнего угла,
void print_sz() - печать ширины и высоты,
int area() - возвращает площадь прямоугольника,
int perimeter() - возвращает периметр прямоугольника,
bool intersect(Rect r) - возвращает истину, если данный прямоугольник имеет хотя бы одну общую точку с прямоугольником r.

Output format

Формат вывода координат прямоугольника: (%d,%d)\n
Формат вывода ширины и высоты прямоугольника: wd=%d hd=%d\n

Examples

Input

Например, вместе с описание класса должен компилироваться и работать следующий код.

int main()
{
     Rect r1(-2, -3, 4, 5);
     Rect r2;
     Rect r3(7);
     r1.print_corner();
     r1.print_sz();

     int a2 = r2.area();
     int p2 = r2.perimeter();
        cout << a2 << " " << p2 << endl;

     if (r1.intersect(r3))
        cout << "YES";
     else
        cout << "NO";

     return 0;
}

Output

(-2,-3)
wd=4 hd=5
1 4
YES
