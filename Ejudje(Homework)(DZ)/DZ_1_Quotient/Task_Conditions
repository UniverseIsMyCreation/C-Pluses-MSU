Обыкновенная дробь

Привести описание класса "обыкновенная дробь" Quotient, члены-данные класса – числитель (целое число типа int) и знаменатель 
(натуральное число типа unsigned). При создании объектов этого класса возможно задание значений числителя и знаменателя или только 
числителя (знаменатель в этом случае считается равным 1). По умолчанию создается дробь с числителем 0 и знаменателем 1. 
При создании дроби должно проводиться сокращение, то есть дроби 1/2 и 6/12 должны стать одинаковыми. 
В случае если знаменатель дроби равен 0, принудительно заменить его на 1. 
Класс Quotient должен содержать метод print(), который распечатывает обыкновенную дробь.

Output format

Вывод дроби должен быть в формате %d/%u, если знаменатель не равен 1 и в формате %d, если знаменатель равен 1.

Examples

Input

Например, должен работать следующий код:

int main()
{
       Quotient q1(1,2), q2(6,12), q3(7), q4;
       q1.print();
       q2.print();
       q3.print();
       q4.print();
       return 0;

}

Output

1/2
1/2
7
0